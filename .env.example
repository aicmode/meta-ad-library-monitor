# 外部ユーザーへデモURLを共有するときは true にします。
# 削除禁止・重複登録防止・手動チェックのクールダウン・安全なエラー表示が有効になります。
# ローカルで管理作業をするときは false（または未設定）にします。
DEMO_MODE=false

# SQLiteファイルの保存先。未設定なら data/monitor.db を使います。
# デプロイ先では永続ボリューム上のパスを指定してください（例: /data/monitor.db）。
# DATABASE_PATH=data/monitor.db
